---
name: index
eleventyComputed:
    title: "{{langs[L].pages[name].title}}"
    alt:
      es: "{{langs.es.folder}}/"
      en: "{{langs.en.folder}}/"
    description: "{{langs[L].pages[name].description}}"
    permalink: "{{langs[L].folder}}/{{langs[L].pages[name].url}}.html"
layout: shared/layouts/base.html
pagination:
    data: langs
    size: 1
    alias: L
    addAllPagesToCollections: true  

date: Last Modified
changefreq: "weekly"
priority: "1.0"
tags: page
---
<!-- Additional head content -->
{% block head %}
<link rel="stylesheet" href="/assets/css/home.css">
{% endblock %}

<!-- Main -->
<main class="main">
  <div class="container-main">

    <!-- Cabecera -->
    {% include 'shared/sections/home/main-header.html.njk' %}

    <!-- ... -->
    {% include 'shared/sections/home/first-contain.html.njk' %}

    <!-- Locations -->
    {% include 'shared/sections/home/locations.html.njk' %}

    <!-- Pisos -->
    {% include site + '/sections/home/flats.html.njk' %}
   
    <!-- Servicios -->
    {% include 'shared/sections/home/services.html.njk' %}

    <!-- Cotownity -->
    <!--?
    {% include 'shared/sections/home/cotownity.html.njk' %}
    -->
   
    <!-- Partners -->
    {% include 'shared/sections/home/partners.html.njk' %}

    <!-- Opinios -->
    {% include 'shared/sections/home/testimonial.html.njk' %}

  </div>
</main>

{% block footer %}
<script>
new Splide('.splide-partners', {
  type: 'loop',
  fixedWidth: '240px',
  focus: 0,
  perMove: 1,
  drag: 'free',
  arrows: false,
  pagination: false,
  pauseOnHover: false,
  pauseOnFocus: false,
  autoScroll: { speed: 2 }
}).mount( window.splide.Extensions );

new Splide(".splide-locations", {
  type: 'loop',
  height: '360px',
  focus: 0,
  perPage: 3,
  perMove: 1,
  padding: '5em',
  gap: '15px',
  breakpoints: {
    1280: { perPage: 2 },
    1024: { padding: '3em', height: '300px' },
    768: { perPage: 1 },
  },
}).mount();  

new Splide(".splide-opinions", {
  type: 'slide',
  focus: 0,
  height: 340,
  perPage: 3,
  perMove: 1,
  padding: '10em',
  gap: '20px',
  breakpoints: {
    1600: { perPage: 2 },
    1024: { perPage: 1 },
    768: { padding: 0 },
  },
}).mount();  
</script>
{% endblock %}
